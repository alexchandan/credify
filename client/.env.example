# Browser requests use the same-origin Next.js proxy by default.
NEXT_PUBLIC_API_BASE_URL=/api/v1

# Absolute backend API target used by the Next.js rewrite.
API_PROXY_TARGET=http://localhost:5000/api/v1
